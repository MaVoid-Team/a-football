import { PlayerRegisterForm } from "@/components/player/player-register-form";

export default function PlayerRegisterPage() {
    return (
        <div className="min-h-screen flex items-center justify-center px-6 py-20">
            <PlayerRegisterForm />
        </div>
    );
}
